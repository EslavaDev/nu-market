import { StyleSheet } from 'react-native';

export const stylesCategoryOptions = StyleSheet.create({
  wrapper: {
    display: 'flex',
    flexDirection: 'row',
    margin: 20,
  },
  options: {
    backgroundColor: '#ffffff',
    borderRadius: 20,
    width: 40,
    height: 40,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginHorizontal: 10,
  },
});
